# Verdict T-005 — PASS
Reviewed: 6ee74713e57d  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — `AppTest.from_file("app/pages/customer_health.py", default_timeout=60).run()` produced `at.exception` empty (no exception); independent probe found 9 `plotly_chart` elements (>=8) and 7 `metric` elements (>=4); rendered text contains "assumed" and "How the health score is calculated" (verified via tests/test_page_customer.py::test_fr011_customer_page_renders_with_charts_metrics_and_assumed_language, 5 passed, and my own AppTest probe).
- AC2: PASS — grepped app/pages/customer_health.py for `relationship`, `confidence`, `margin`, `competitive`, `stakeholders`, `dependencies`, `support_burden`, `decisions_needed`, `internal_composite`, `Audience.INTERNAL`: zero matches. Also confirmed by tests/test_page_customer.py::test_fr011_customer_page_source_has_no_internal_identifiers (passed).
- AC3: PASS — independent AppTest probe collected all rendered markdown/caption/text/info/metric-label/metric-value/metric-delta/dataframe(.to_string())/expander-child text and regex-checked (word-boundary, case-insensitive) the full C-06 list (confidence, margin, competitor, competitive, renewal, churn, sentiment, blocker, arr, gross margin, unbilled, incumbent, ccaas, einstein, gorgias ai, freddy) plus literal strings "210000", "Einstein", "CCaaS": zero matches. tests/test_page_customer.py::test_fr011_customer_page_rendered_text_has_no_internal_vocabulary also passed (its own C-06 subset).
- AC4: PASS — metric values include "70.8" (Tier-1 automation exit day tile), "7.35" (AHT tile), "84.2" (CSAT tile); rendered text contains "67.9" and "assumed" (AHT/CSAT baseline captions, e.g. app/pages/customer_health.py:77 "assumed pre-deployment baseline"). None of these figures are hand-typed in the page source — grep for "70.8|84.2|7.35|67.9" in app/pages/customer_health.py returns no matches; all values flow from `view.claims`/`AccountView` fields formatted with f-strings. tests/test_page_customer.py::test_fr011_customer_page_tiles_show_headline_and_caveat_figures passed.
- AC5: PASS — independent AppTest run with `LODESTAR_INTERNAL_ENABLED=0` set produced no exception and 9 plotly_chart elements. tests/test_page_customer.py::test_nfr005_customer_page_renders_without_internal_enablement passed.

## Test / gate output
- `uv run pytest tests/test_page_customer.py -q` → 5 passed
- `uv run ruff check app tests && uv run ruff format --check app tests` → "All checks passed!" / "29 files already formatted"
- `uv run python scripts/check.py` → secrets scan clean, ruff check/format clean, mypy (lodestar + linux) clean, full suite 142 passed, coverage 97.71% (>=80% required), "all checks passed"

## §14 section order (source read top-to-bottom, app/pages/customer_health.py `main()` lines 315-330)
Header (title+caption) -> assumptions banner -> four tiles (automation exit w/ month-mean caption, AHT w/ assumed-baseline caption, CSAT likewise, operational health score w/ band badge) -> usage dual-axis (requests, automated tickets, incident shading) -> reliability row (availability w/ SLO hline, error rate, P50/P95 w/ P95-budget hline) -> quality row (grounding, eval pass, escalation) immediately followed by "Known caveats" expander with claims table (claim/status/arithmetic/caveat) -> spend row (daily spend w/ budget-pace hline, month total vs budget, cost per automated ticket, projected next month flat vs at-growth) -> "Risks and recommended actions" (risks w/ mitigation, then "Joint next steps" owner/due table) -> "Expansion opportunity" (brand table w/ customer-only fields, readiness progress bar = fit score, pilot card w/ six success criteria: automation rate, CSAT delta, grounded-answer rate, P95 latency, availability, escalation rate) -> "How the health score is calculated" expander (pillar table floors/targets/weights/observed/score + methodology_note arithmetic). Matches docs/design/03-lld.md §14 "Customer page order (FR-011)" paragraph exactly.

## Verification checklist
- [x] scope respected — `git diff HEAD~1 --stat` touches only app/pages/customer_health.py, tests/test_page_customer.py, and docs/tasks/T-005-customer-page.md (Handoff section only, per workflow); app/main.py and app/components/* untouched.
- [x] no hand-typed business numbers — grepped all digit literals in app/pages/customer_health.py; remaining literals are `st.columns(3/4)` layout counts, list/column indices, format specifiers (`:.1f`, `:.2f`, `:.0f`, `:.4f`), field-name substrings (`p50`/`p95`/`30d`), and the fixed 0-100 scale used for `pilot.fit_score` (progress-bar normalization and "/100" denominator — `fit_score()` in lodestar/brands.py:85-88 is defined as `round(total * 20, 1)` on a 0-5 weighted average, i.e. an inherent 0-100 scale, not a hand-typed business figure). No AC4 figures (70.8/7.35/84.2/67.9) appear as literals in the page source.
- [x] no internal attribute access — page builds only `Audience.CUSTOMER` view (line 30); grep for the forbidden-identifier list (relationship, confidence, margin, competitive, stakeholders, dependencies, support_burden, decisions_needed, internal_composite, Audience.INTERNAL) returns zero matches; brand table columns (Brand, Category, Tier-1 tickets/day, Support tooling, Readiness, Proposed sequence, Proposed window) match only the customer-visible `BrandProfile` fields per lodestar/brands.py:37-62, not the 9 internal-only fields listed there (stage, confidence_pct, est_monthly_inference_usd, est_annual_value_usd, blockers, competitive_note, margin_note, internal_owner, target_date).
- [x] tests named with RTM IDs — all 5 new tests in tests/test_page_customer.py are prefixed `test_fr011_` (4 tests) or `test_nfr005_` (1 test), matching the pack's rtm: [FR-011, NFR-005].
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty.

## Findings
1. none
