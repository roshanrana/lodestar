# Verdict T-004 — PASS
Reviewed: b4bfb8b36dac  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — `line_with_incidents` returns a `go.Figure`; probe with 2 incident dates + hline produced 2 `rect` shapes and 1 `line` shape (scratchpad probe.py output: `rects=2 lines=1`); `tests/test_charts.py:57-84` (`test_fr010_line_with_incidents_returns_figure_with_incident_vrect_and_hline`, `test_fr010_line_with_incidents_adds_one_vrect_per_incident_date`) assert the same via `fig.layout.shapes`.
- AC2: PASS — `pillar_bar(score(load()))` returned 5 bars whose `bar.x` == `[p.score for p in report.pillars]` exactly (probe output: both lists identical, e.g. `[80.17857142857142, 87.17460317460247, ...]`) with reference `vline`s at x=65.0 and x=80.0 (`app/components/charts.py:183-184`, `_PILLAR_WATCH_THRESHOLD`/`_PILLAR_HEALTHY_THRESHOLD`); `tests/test_charts.py:171-185`. `fit_confidence_scatter` on real brands from `rank_pilots(load_context(Path("data")).brands)` plotted a `scatter` trace; the same brands with every `confidence_pct` set to `None` via `model_copy(update=...)` fell back to a `bar` trace (probe output: `trace0 type: scatter` then `trace0 type: bar`); mirrored by `tests/test_charts.py:188-207`.
- AC3: PASS — `dual_axis` probe confirmed `fig.layout.yaxis2 is not None` and both series present; `tests/test_charts.py:101-119` asserts `yaxis`/`yaxis2` titles and both trace names.
- AC4: PASS — every probed figure (`line`, `dual`, `pillar`, `fit_real`, `fit_none`) had `template="plotly_white"`, `height=320`, `margin=(l=10,r=10,t=40,b=10)`, `legend.orientation="h"`, `hovermode="x unified"` (probe output lines); palette constants `PRIMARY=#2563eb`, `SECONDARY=#0d9488`, `WARN=#d97706`, `BAD=#dc2626`, `MUTED=#64748b` match LLD §14 exactly (probe output: `PALETTE: #2563eb #0d9488 #d97706 #dc2626 #64748b plotly_white`); also `tests/test_charts.py:139-165`.
- AC5: PASS — `tiles.py` (`kpi_tile`, `band_badge`, `assumption_banner`) run inside `streamlit.testing.v1.AppTest` via a temp page file exactly as the pack specifies; `uv run pytest tests/test_charts.py -q` → 15 passed, including `test_fr011_tiles_render_without_exception_inside_apptest` (`tests/test_charts.py:247-255`), which asserts `not at.exception`, 2 metrics and 1 info box rendered.

## Verification checklist
- [x] scope respected — `git diff HEAD~1 --stat` touches only `app/components/charts.py`, `app/components/tiles.py`, `tests/test_charts.py`, plus the task pack's own Handoff section (`docs/tasks/T-004-chart-components.md`, the implementer-filled block the pack template designates for this purpose) — matches the pack's Scope list.
- [x] no hard-coded business numbers — only layout/palette constants (`_CHART_HEIGHT=320`, `_CHART_MARGIN`, `PRIMARY`/`SECONDARY`/`WARN`/`BAD`/`MUTED`, shading rgba strings) and the pack-authorized `_PILLAR_WATCH_THRESHOLD=65.0` / `_PILLAR_HEALTHY_THRESHOLD=80.0` band thresholds appear in `app/components/charts.py:21-37`; `app/components/tiles.py` has no numeric business constants at all.
- [x] no sensitive fields logged — `grep -n "log\|print("` over `app/components/charts.py` and `app/components/tiles.py` returned no matches; neither file imports `logging` or calls `print`.
- [x] tests named with RTM IDs — all 14 test functions in `tests/test_charts.py` start with `test_fr010_` or `test_fr011_` (verified via `grep -n "^def test_" tests/test_charts.py`), matching `rtm: [FR-010, FR-011]` in the pack.
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty.

## Findings
1. none
