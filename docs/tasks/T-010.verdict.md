# Verdict T-010 — FAIL
Reviewed: 78f0a5933136  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — README.md contains every submission §2 bullet as a heading/table row (`## At a glance` Stack row for deps/versions; `## Quick start` for install/start; `## Where the CSV goes`; `## Views`; `## Configuration`; `## Known limitations`). Ran every Quick-start command from the worktree: `uv run lodestar dashboard --port 8512` → `GET /customer_health` 200, `GET /internal_qbr` 200 (urllib, then process killed); `uv run lodestar api --port 8767` → `GET /healthz` → `200 {"status":"ok"}` (process killed); `uv run lodestar mcp --help` printed usage; `uv run lodestar ebr --out <tmp>` wrote `northstar-ebr-brief.md`/`.docx`; `uv run python scripts/check.py` → "all checks passed" (251 passed, 97.47% coverage). Every env var in README's Configuration table (`LODESTAR_CSV`, `LODESTAR_DATA_DIR`, `LODESTAR_INTERNAL_ENABLED`, `LODESTAR_LLM_PROVIDER`, `FIREWORKS_API_KEY`, `LODESTAR_FIREWORKS_MODEL`, `LODESTAR_FIREWORKS_BASE_URL`, `ANTHROPIC_API_KEY`, `LODESTAR_ANTHROPIC_MODEL`, `LODESTAR_API_PORT`, `LODESTAR_DASHBOARD_PORT`) matches name and default in `lodestar/config.py` lines 16-22, 35-45.
- AC2: PASS (content) — `docs/design-document.md` has a section for every submission §3 bullet (screenshots internal/customer, audience/purpose table, why-these-metrics, health-score calculation, internal-vs-customer diff table, intentionally-excluded, assumptions/caveats/trade-offs incl. paraphrase-leak residual and F7/L7 choice). Every PNG in `docs/assets/` starting `customer-` or `internal-` (`customer-account-health.png`, `internal-qbr.png`) is embedded; all referenced image paths (`assets/home.png`, `assets/internal-qbr.png`, `assets/customer-account-health.png`) exist. Health-score table reproduces weights/floors/targets and shipped pillar scores 80.2/87.2/78.5/83.1/65.8, composite 80.2, cross-checked against `uv run lodestar score --json` (adoption 80.1786→80.2, reliability 87.1746→87.2, latency 78.4625→78.5, quality 83.0688→83.1, spend_efficiency 65.7908→65.8, operational_composite 80.2345→80.2). See Finding 1: this file was not modified by T-010's own commit.
- AC3: PASS — `uv run python metrics/render.py --check` → "metrics card is current"; `git diff HEAD~1 -- README.md` shows the `<!-- metrics:start -->`/`<!-- metrics:end -->` block unchanged (diff hunks only add content immediately before the start marker and immediately after the end marker).
- AC4: PASS — grepped C-06 terms (confidence, margin, competitor, competitive, renewal, churn, sentiment, blocker, ARR, incumbent) in README.md and docs/design-document.md. All real hits fall either inside the "Internal · QBR" paragraph/section (README.md:113; design-document.md's "Screenshots — internal QBR" section and "Internal relationship extension" subsection, design-document.md:21-25,153), inside the internal column of the "How internal and customer content differs" comparison table (design-document.md:173), or inside "Customer view excludes" (design-document.md:187-189). Remaining hits are false-positive substring matches ("carry", "narrative", "narrows") not the flagged terms.

## Verification checklist
- [ ] scope respected — README.md is in-scope and correctly edited outside the metrics markers. However, the pack's other in-scope deliverables (`docs/design-document.md`, `docs/ops/runbook.md`, the `docs/mcp.md` extension) are **not present in this task's diff at all** — see Finding 1.
- [x] every referenced image exists — `docs/assets/home.png`, `customer-account-health.png`, `internal-qbr.png` all present.
- [x] commands verified by running them — all Quick-start commands and `scripts/check.py` executed above.
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty.

## Findings
1. (blocking) `git diff HEAD~1 --stat` at the reviewed commit (78f0a5933136) shows only `README.md` (183 insertions) and `docs/tasks/T-010-docs.md` (6-line Handoff addition) changed — it does **not** touch `docs/design-document.md`, `docs/ops/runbook.md`, or `docs/mcp.md`. Yet the pack's own Handoff (docs/tasks/T-010-docs.md:51-52) states "Files changed: README.md, docs/design-document.md (new), docs/ops/runbook.md (new), docs/mcp.md" — false for three of the four files. `git log --oneline -- docs/design-document.md docs/ops/runbook.md docs/mcp.md` shows these files were actually added/extended in commit 7d36723 ("T-012: verdict PASS; evidence seq 20", `git diff 7d36723 HEAD -- docs/design-document.md docs/ops/runbook.md docs/mcp.md` is empty, confirming no change since). That commit's own task pack, `docs/tasks/T-012-polish-followups.md`, scopes only `app/pages/*.py`, `app/components/charts.py`, `lodestar/data.py` (docstring), `lodestar/view.py`, `lodestar/narrative/ebr.py`, `docs/ebr/*`, and specific test files — it does not list `docs/design-document.md`, `docs/ops/runbook.md`, or `docs/mcp.md`. So the content that satisfies AC2 and the runbook/mcp accuracy checks exists in the tree, but it was produced and committed outside both the task that claims it (T-010, this diff) and the task whose commit actually carries it (T-012, whose own pack does not scope these files either). This is an evidence-provenance/audit-trail break in a Shipyard-governed repo where per-task verified diffs are the unit of trust — the Verifier for T-010 cannot attribute this content to a task pack and verdict that actually cover it.
2. (non-blocking) `docs/ops/runbook.md`'s error-code table (all 11 codes: DATA_NOT_FOUND, DATA_TOO_LARGE, DATA_SCHEMA, DATA_VALUE, DATA_MULTI_DEPLOYMENT, DATA_DUPLICATE_DATE, CONTEXT_INVALID, PROVIDER_CONFIG, PROVIDER_HTTP, LEAK_GUARD, INTERNAL_DISABLED) matches `lodestar/errors.py`'s `Code` enum exactly, and `docs/mcp.md`'s registration snippets (`uv run --directory <repo> lodestar mcp`, `claude mcp add lodestar -- uv run --directory <repo> lodestar mcp`) match `lodestar/commands/mcp.py`'s actual invocation — no inaccuracies found, but this content could not be checked against a verified task pack per Finding 1.
