# Verdict T-001 — PASS
Reviewed: 018d780c1a28  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — `field_visibility(Branch, "untagged")` is INTERNAL (tests/test_audience.py:60-61, and reproduced live: `field_visibility(Fresh, "plain")` → `internal`). `scrub(model, CUSTOMER)` on the 3-level nested fixture (tests/test_audience.py:87-102, `Branch{leaf, leaves: tuple, leaf_by_key: dict}`) clears `secret_note`/`internal_amount` at every depth while `public_note`/`name` stay identical (test_fr009_scrub_customer_removes_internal_fields_recursively, PASS). Reproduced independently on a Top→Mid→{tuple,dict}→Leaf fixture: `scrubbed.top_secret`/`mid.mid_secret`/tuple+dict `.sec` all `None`; customer fields (`name`, `pub`) unchanged. `scrub(model, INTERNAL)` returns an equal object (test_fr009_scrub_internal_returns_equal_object, lodestar/audience.py:73-74 `if audience is Audience.INTERNAL: return obj`).
- AC2: PASS — `internal_field_count(Branch) == 3` (tests/test_audience.py:119-122, hand count: `internal_amount` + `untagged` on Branch + `secret_note` on Leaf, counted once despite 3 reachability paths). On my own Top/Mid/Leaf fixture, `internal_field_count(Top) == 3` matches hand count (top_secret, mid_secret, sec). `internal_field_count(BrandProfile) == 9` (tests/test_audience.py:125-126; lodestar/brands.py:63-72 lists exactly 9 internal fields: stage, confidence_pct, est_monthly_inference_usd, est_annual_value_usd, blockers, competitive_note, margin_note, internal_owner, target_date). Handoff states the number (9, no fit_weights_note added) — matches.
- AC3: PASS — `load_context(DATA_DIR)` succeeds on shipped `data/` (test_fr007_load_context_succeeds_on_shipped_data). Unknown top-level key in assumptions.yaml raises `CONTEXT_INVALID` naming file+field (test_fr007_unknown_key_in_assumptions_raises_context_invalid; reproduced live: `code=CONTEXT_INVALID detail={'file': 'assumptions.yaml', 'field': 'bogus_key'}`); same pattern verified for account_context.yaml and brands.yaml (root and nested-entry unknown keys), all via `lodestar/context.py:227-236` `_check_root_keys` and `_validation_error` (extra="forbid" ConfigDict on every model). Every internal-tagged field on every context model is Optional — test_fr009_all_internal_fields_optional_on_context_models (offenders == []) plus my own walk over Context/AssumptionSet/.../BrandProfile/BrandCatalog/FlagshipBrand/Risk/NextAction: offenders == [].
- AC4: PASS — fit scores {Kestrel 87.0, Little Compass 60.0, Lumen Beauty 58.0, Meridian Home 57.0} reproduced live from `load_context(DATA_DIR)` and match test_fr008_fit_score_matches_expected_values_from_shipped_data. `rank_pilots(ctx.brands)` order reproduced live as (Kestrel Athletics, Little Compass, Lumen Beauty, Meridian Home), matching test_fr008_rank_pilots_orders_kestrel_first_under_shipped_weights. Formula and tie-break also unit-tested (test_fr008_fit_score_formula_is_round_sum_weight_times_dimension_times_20 → 80.0; test_fr008_rank_pilots_breaks_ties_by_name → Alpha, Mu, Zeta).
- AC5: PASS — `static_risks(ctx)` ids `[R-REL-1, R-COMP-1, R-EXEC-1, R-TECH-3]` with visibilities `[internal, internal, customer, customer]` (test_fr007_static_risks_returns_four_expected_ids_and_visibilities, lodestar/risks.py:62-65 returns `ctx.account.risks` verbatim, matching data/account_context.yaml risk visibilities). `next_actions(ctx)` returns 8 items, 5 customer-visible / 3 internal (test_fr007_next_actions_returns_eight_with_five_customer_visible; hand count of data/account_context.yaml `next_actions:` list confirms 5 `visibility: customer` + 3 `visibility: internal`).
- AC6: PASS — `ctx.account.relationship` loads as `dict[str, float]` = `{executive_sponsor_engagement: 80, champion_strength: 85, stakeholder_coverage: 55, renewal_outlook: 75, competitive_position: 60}`, mean 71.0 (test_fr004_relationship_loads_as_internal_dict_with_mean_71; (80+85+55+75+60)/5 = 71.0). `field_visibility(AccountContext, "relationship")` is INTERNAL (lodestar/context.py:169, tagged `Visibility.INTERNAL`).

## Verification checklist
- [x] scope respected — `git diff HEAD~1 --name-only` touches exactly lodestar/{audience,context,brands,risks}.py, tests/test_{audience,context,brands,risks_static}.py, and docs/tasks/T-001-audience-context-brands-risks.md (the task pack's own Handoff section, expected process artifact) — matches pack Scope exactly, no out-of-scope files.
- [x] error taxonomy used — `Code.CONTEXT_INVALID` (lodestar/errors.py:23) is the sole error path for all load/validation failures in lodestar/context.py (`_load_yaml`, `_validation_error`, `_check_root_keys`); no bare exceptions or ad hoc error types introduced.
- [x] no sensitive fields logged — `grep -n "print(\|logging\.\|logger\."` over the four changed source files returns no matches; no logging/print statements anywhere in the diff.
- [x] tests named with RTM IDs — all 27 new test functions across the four test files are prefixed `test_fr004_`, `test_fr007_`, `test_fr008_`, `test_fr009_`, or `test_nfr005_`, exactly covering pack `rtm: [FR-004, FR-007, FR-008, FR-009, NFR-005]`.
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty.
- [x] every internal field Optional and tagged; default visibility INTERNAL — confirmed structurally by `test_fr009_all_internal_fields_optional_on_context_models` and my own recursive walk (see AC3); `field_visibility()` defaults untagged fields to `Visibility.INTERNAL` (lodestar/audience.py:49-63, confirmed live and by test_fr009_field_visibility_defaults_to_internal_when_untagged). `field_visibility()` also correctly reads T-002's literal `json_schema_extra={"visibility": "internal"}` style (test_fr009_field_visibility_reads_literal_json_schema_extra; live check on `HealthReport` correctly flags its 4 documented internal fields — relationship_inputs, relationship_score, internal_composite, internal_band — as internal, per lodestar/health.py:161-174).

## Validation commands run
- `uv run pytest tests/test_audience.py tests/test_context.py tests/test_brands.py tests/test_risks_static.py -q` → 27 passed
- `uv run mypy lodestar` → Success: no issues found in 15 source files
- `uv run ruff check . && uv run ruff format --check .` → All checks passed! / 37 files already formatted
- `uv run python scripts/check.py` → ruff check, ruff format --check, mypy (native + linux), pytest (78 passed, 97.11% coverage), secrets scan — all checks passed

## Findings
1. (non-blocking) `HealthReport` (lodestar/health.py, T-002 scope, not touched by this diff) has several fields (`window_days`, `pillars`, `operational_composite`, `operational_band`, `weakest_pillar`, `methodology_note`) that carry no visibility tag at all and therefore fall back to `Visibility.INTERNAL` under the fail-closed default, even though by content they read as generally-shareable operational data. This is outside T-001's scope (audience.py's mechanism is correctly fail-closed and behaves exactly as designed) and does not affect any T-001 acceptance criterion; flagging only for whoever next touches `AccountView`/`view.py` (T-003) to confirm intent before those fields reach a customer-facing surface.
