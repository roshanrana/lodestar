# Verdict T-006 — PASS
Reviewed: f153826694bc  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — `AppTest.from_file("app/pages/internal_qbr.py", default_timeout=60).run()` completed with no exception (`at.exception` empty); `len(at.get("plotly_chart")) == 6` (≥6); `len(at.metric) == 4`; banner text "INTERNAL — Fireworks only. Not for customer distribution." found in `at.error` output. Also asserted in `tests/test_page_internal.py:52-55` (`test_fr010_internal_page_renders_with_tiles_charts_and_banner`), passing.
- AC2: PASS — rendered text contains "77.5" (internal composite tile, `app/pages/internal_qbr.py:53`; `view.health.internal_composite == 77.46417946129188` → `.1f` → "77.5", within ±0.3 of spec value); "Kestrel Athletics" present (expansion pipeline table, `app/pages/internal_qbr.py:230`); pipeline dataframe shows column "Confidence %" with Kestrel Athletics row value 75.0 (`app/pages/internal_qbr.py:233`); the three decision owners "Fireworks sales leadership", "Fireworks product", "Account lead" all found in rendered text (decisions-needed panel, `app/pages/internal_qbr.py:277-280`). Also asserted in `tests/test_page_internal.py:58-72` (`test_fr010_internal_page_shows_health_pipeline_and_decision_owners`), passing.
- AC3: PASS — with `LODESTAR_INTERNAL_ENABLED=0` set before import, `at.exception` empty, `len(at.get("plotly_chart")) == 0`, one warning rendered: "Internal QBR is disabled for this environment (LODESTAR_INTERNAL_ENABLED=0)..." (`app/pages/internal_qbr.py:339-341`, gate uses `Settings.from_env().internal_enabled` then `st.warning` + `st.stop()`). Also asserted in `tests/test_page_internal.py:75-84` (`test_fr010_internal_disabled_shows_notice_and_no_charts`), passing.
- AC4: PASS — risk card titles appear in text at positions R-EXEC-1 (757) < R-TECH-1 (963) < R-REL-1 (1182), confirmed exact titles: "R-EXEC-1 — Four brands, four help-desk stacks, four operating teams", "R-TECH-1 — Usage growth is decoupled from ticket volume", "R-REL-1 — Stakeholder coverage is thin beyond the CX organisation" (`app/pages/internal_qbr.py:211-223`, iterates `view.top_risks` in order). Also asserted in `tests/test_page_internal.py:87-98` (`test_fr010_top_three_risk_cards_are_in_expected_order`), passing.
- AC5: PASS — next-actions dataframe has 8 rows and a "Visibility" column (`app/pages/internal_qbr.py:292-303`, columns Action/Owner/Due/Visibility). Also asserted in `tests/test_page_internal.py:101-113` (`test_fr010_next_actions_table_has_eight_rows_and_visibility_column`), passing.

Section order confirmed top-to-bottom by reading `app/pages/internal_qbr.py:335-359` and probing rendered `at.subheader`: banner (`st.error`, line 335) → gate check (lines 339-341) → title/caption (345-349) → tiles (351) → "Health breakdown (L7)" (352) → "Trends" (353, five charts: adoption, spend, reliability, latency, quality — `_chart_adoption`/`_chart_spend`/`_chart_reliability`/`_chart_latency`/`_chart_quality` at lines 110-208, matching §14's listed order) → "Top risks" (354) → "Expansion pipeline" (355, table + `fit_confidence_scatter`) → "Dependencies, support, competitive notes, decisions" (356, four `st.columns`) → "Next actions" (357) → "Stakeholder coverage" (358) → "Claims check" (359). This matches `docs/design/03-lld.md` §14 lines 401-408 exactly.

Test run: `uv run pytest tests/test_page_internal.py -q` → 5 passed. `uv run ruff check app tests` → all checks passed. `uv run ruff format --check app tests` → 28 files already formatted. Full gate `uv run python scripts/check.py` → ruff check/format, mypy (both platforms), pytest (137 passed, 97.71% coverage, threshold 80%), secrets scan → "all checks passed" (this worktree contains only T-006 on top of the verified baseline, no T-005 customer page present, so the full gate is clean as expected).

## Verification checklist
- [x] scope respected — `git diff HEAD~1 --name-only` shows only `app/pages/internal_qbr.py`, `tests/test_page_internal.py`, `docs/tasks/T-006-internal-page.md` (Handoff section only, per template); pack scope is `app/pages/internal_qbr.py` + `tests/test_page_internal.py`. `git diff HEAD~1 --stat -- app/main.py app/components/` is empty — both untouched.
- [x] no hand-typed business numbers — `grep -n '[0-9]' app/pages/internal_qbr.py` shows only layout/formatting constants (`st.columns(4)`, `timedelta(days=1)`, `_WINDOW_LENGTH_DAYS = 7`, `.1f`/`round(x, 3)`/`round(x, 1)` formatting, "month 1" field-name labels, the risk-ID literal `"R-COMP-1"` used for a lookup) — every rendered metric value is read from `view`/`health`/`brand`/`risk`/`decision` attributes, never a literal business figure.
- [x] internal-enabled gate respected — `app/pages/internal_qbr.py:337-341` reads `Settings.from_env().internal_enabled` and calls `st.warning(...)` + `st.stop()` before any data view is built or chart rendered when disabled; confirmed via probe (0 plotly_chart, 1 warning) and `test_fr010_internal_disabled_shows_notice_and_no_charts`.
- [x] tests named with RTM IDs — all five test functions in `tests/test_page_internal.py` are prefixed `test_fr010_` (FR-010 per task pack `rtm: [FR-010]`).
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty.

## Findings
1. none
