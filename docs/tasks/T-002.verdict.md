# Verdict T-002 — PASS
Reviewed: e67be262425f  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — All named §3 anchors reproduced within tolerance: `test_fr003_window_mean_and_window_sum_on_shipped_dataset` (Σ requests 789563, exact), `test_fr003_month_totals_match_lld_anchors` (Σ spend 1213.12±0.01, cost_per_automated_ticket_month 0.01244±0.0001), `test_fr003_l7_growth_ratios_match_lld_anchors` (spend_growth_ratio 1.436, automated_growth_ratio 1.147, spend_vs_outcome_ratio 1.253, all ±0.01). Independent recompute direct from `data/northstar_flagship_30_day_metrics.csv` (own script, not via package) reproduced every listed anchor exactly: Σrequests=789563, Σspend=1213.12, Σautomated=97488, Σtier1=143430, L7 automation=70.0714, L7 availability=99.8997, L7 p95=1463.14, spend_growth=1.4364, automated_growth=1.1465, spend_vs_outcome=1.2528, cost/automated(month)=0.012444. requests_per_ticket (4.345/6.548) and tokens_per_request (1398.0) are derived `DayRow` fields from T-000 scope and already covered by `tests/test_data.py:27-29`.
- AC2: PASS — `test_fr006_metric_score_matches_hand_computation_for_two_metrics` gives automation 75.4±0.3, p95 79.8±0.3 (hand calc: 75.36 and 79.76, both within tolerance). Clipping at both ends tested in `test_fr006_metric_score_clips_at_both_ends` (literal ±1000 inputs) and `test_fr006_metric_score_clips_with_synthetic_extreme_rows` (synthetic CSV rows via `tmp_path`, both directions).
- AC3: PASS — `test_fr006_pillar_and_composite_anchors_on_shipped_dataset` reproduces adoption 80.2, reliability 87.2, latency 78.5, quality 83.1, spend_efficiency 65.8, composite 80.2/healthy, all ±0.3. Independent recompute: adoption 80.18, reliability 87.17, latency 78.46, quality 83.07, spend_efficiency 65.79, composite 80.235 — matches package output from `score(load())` exactly (adoption 80.179, reliability 87.175, latency 78.462, quality 83.069, spend_efficiency 65.791, composite 80.235, band healthy). Band boundaries tested in `test_fr006_band_thresholds_at_boundaries` (79.99→watch, 80.0→healthy, 64.99→at_risk, 65.0→watch).
- AC4: PASS — `test_fr006_score_without_relationship_has_no_internal_fields` confirms `relationship_score is None` etc. `test_fr006_score_with_relationship_matches_internal_anchors` gives relationship_score 71.0±0.3, internal_composite 77.5±0.3, band watch. Independent recompute with inputs {80,85,55,75,60}: mean 71.0, internal_composite 0.7×80.235+0.3×71.0=77.464 — matches package output (`score(ds, relationship=rel)` → rel_score 71.0, internal_composite 77.464, band watch) exactly.
- AC5: PASS — `test_fr006_detect_flags_exactly_the_two_known_incident_days` and `test_fr006_detect_matches_note_incident_dates_with_no_false_positives_or_misses` assert flagged={2026-08-09, 2026-08-21}, matched=2, false_positives=0, missed=0. Independent recompute of the §6 rule (≥3 prior days, error>1.30×median(prior≤7) or p95>1.20×median(prior≤7)) over the raw CSV reproduces the identical two flagged dates and 27 days below 99.9% availability, matching direct execution of `detect(load(), 99.9)` → `days_below_slo=27`. Per orchestrator ruling, 27 (not the pack's original 22) is the correct anchor after the architect's LLD §6 correction; asserting 27 is correct, not a defect. Note (non-blocking, see Findings #1): the test asserting `days_below_slo` recomputes with the same field/predicate as production code rather than a literal `== 27`, but direct execution independently confirms the value is 27.
- AC6: PASS — `test_nfr002_score_on_shipped_dataset_completes_under_one_second` present and passing (full suite run completed in 6.41s for 51 tests, `score()` alone is sub-millisecond on 31 rows).
- AC7: PASS — `summarise(ds: Dataset, budget_line_usd: float)` signature matches spec exactly (`lodestar/spend.py:37`). `test_fr003_all_spend_summary_fields_are_populated` iterates all `model_fields`. `test_fr003_projected_30d_flat_is_l7_daily_mean_times_30` confirms `projected_30d_flat_usd == l7_daily_mean_usd * 30`. `test_fr003_pct_of_budget_uses_the_passed_budget_line` confirms `pct_of_budget` scales inversely with the passed budget argument.

## Verification checklist
- [x] scope respected — `git diff HEAD~1 --stat` touches exactly `lodestar/{stats,spend,health,incidents}.py` and `tests/test_{stats,spend,health,incidents}.py`, matching the pack's Scope section.
- [x] constants match LLD §4 exactly — `test_fr006_method_floors_and_targets_match_lld_table` asserts all 11 metrics' direction/floor/target verbatim against the §4 table; `test_fr006_method_weights_sum_to_one` confirms weights sum to 1.0; manually cross-checked `lodestar/health.py:60-102` (`METHOD`) against §4 table — all values verbatim (0.20/0.25/0.15/0.25/0.15; floors 55/20/99.5/2.0/2500/1000/85/85/70/0.03/1.5; targets 75/10/99.95/0.5/1200/400/97/97/88/0.01/1.0).
- [x] no sensitive fields logged — `grep -in "print(\|logging\|logger"` over `lodestar/{stats,spend,health,incidents}.py` returns no matches; no I/O in any of the four modules (pure functions only).
- [x] tests named with RTM IDs — all 26 new test functions across the four files are prefixed `test_fr003_`, `test_fr006_`, or `test_nfr002_` (`grep` for `^def test_` not matching those prefixes returns zero rows), consistent with `rtm: [FR-003, FR-006, NFR-002]`.
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml` is empty.

## Findings
1. (non-blocking) `tests/test_incidents.py:37-42` (`test_fr006_days_below_slo_counts_real_availability_breaches`) recomputes `days_below_slo` using the same field and threshold predicate (`row.availability_pct < 99.9`) that `lodestar/incidents.py:58` uses internally, rather than asserting a literal `== 27`. This is tautological (would pass even if both implementation and this expectation shared the same latent defect) and doesn't pin the corrected AC5 anchor as a fixed regression guard. Direct execution (`detect(load(), 99.9).days_below_slo`) and independent recomputation from the raw CSV both confirm the value is genuinely 27, so behavior is correct — but a future regression in the availability comparison would not be caught by this specific test. Consider a follow-up to add a literal `assert summary.days_below_slo == 27`.
2. (non-blocking) Two of the §3 anchors listed as "anchors the verifier must reproduce" — Σ automated 97488 and Σ tier1 143430 — have no dedicated literal-value test assertion anywhere in the suite (only indirectly exercised via `cost_per_automated_ticket_month_usd`, which depends on Σautomated). Independently recomputed directly from the CSV and confirmed correct; `tier1_tickets` is not consumed by any T-002 output field, so this is a coverage gap against the letter of §3's anchor list rather than a functional defect.
