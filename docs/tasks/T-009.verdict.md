# Verdict T-009 — PASS
Reviewed: 7264b19c6e6e  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — `uv run python -c "run_bench()==run_bench()"` → `True`. `uv run lodestar bench --check` → exit 0 ("metrics\headline.json is current"). Mutated `kpis.health_score.value` to "99.9 / 100" in `metrics/headline.json`, reran `--check` → "metrics\headline.json is stale", exit 1; restored via `git checkout -- metrics/headline.json` (git status clean after).
- AC2: PASS — `uv run python metrics/render.py --check` → "metrics card is current", exit 0. `git diff HEAD -- docs/assets/metrics.svg` empty (svg matches a fresh render byte-for-byte); README.md has `<!-- metrics:start -->`/`<!-- metrics:end -->` markers (lines 3, 41) with the rendered KPI/bars/facts tables between them; both files are committed at HEAD.
- AC3: PASS — independent `run_bench()` call (bypassing the CLI) returned exactly: health_score "80.2 / 100", claims_verified "1 of 3 from telemetry alone", leak_check "0 of 37" (N=37 ≥ 10), incidents_detected "2 of 2", weakest_pillar "Spend efficiency 65.8", cost_per_automated_ticket "$0.0124" — matches AC3 verbatim. Bar values [80.2, 87.2, 78.5, 83.1, 65.8] cross-checked against a fresh `lodestar.health.score(lodestar.data.load())` call: adoption 80.2, reliability 87.2, latency 78.5, quality 83.1, spend_efficiency 65.8 — identical, in pillar order, accents teal/blue/violet/amber/red per §9.
- AC4: PASS — `lodestar score --json` exit 0, `lodestar claims --json` exit 0, `lodestar risks --audience customer` exit 0, `lodestar pipeline` exit 0 (all verified directly, not just via pytest). `lodestar score --audience customer --json` parsed: `relationship_inputs`, `relationship_score`, `internal_composite`, `internal_band` all `null`. Ran `tests/leakwalk.py::assert_no_internal_populated` directly against `build_view(None, None, Audience.CUSTOMER).health` — passed with no assertion error (0 populated internal fields); ran `count_populated_internal` against the INTERNAL-audience equivalent — returned 4, proving the walk is non-vacuous (it does detect populated internal fields when present).
- AC5: PASS — `uv run python scripts/check.py` ran end-to-end in this clean worktree: 246 tests passed (97.46% coverage, ≥80% floor), ruff check/format clean, mypy clean on both platform targets, secrets scan clean, then the three new steps (`bench`, `bench drift`, `card drift`) all passed, ending "all checks passed". The EBR determinism failure noted in the pack's Handoff did not reproduce here, consistent with the orchestrator's ruling that it was a pre-existing template issue fixed in the parent commit.

## Verification checklist
- [x] scope respected — `git diff HEAD~1 --stat` touches only: `lodestar/bench.py`, `lodestar/commands/{score,claims,risks,pipeline,bench}.py`, `metrics/headline.json`, `scripts/check.py`, `tests/test_bench.py`, `tests/test_commands.py`, `README.md` (new file — title line + marker block only, no other content), and `docs/tasks/T-009-bench-card-cli.md` (Handoff fill-in, +6 lines) — all within the pack's declared scope. `scripts/check.py` diff is STEPS-list-only (three new tuples appended, replacing a placeholder comment). `docs/assets/metrics.svg` shows no diff at HEAD~1..HEAD because its already-committed bytes (from a prior commit) already matched a fresh deterministic render — confirmed current via `render.py --check`.
- [x] headline.json has no timestamps or machine-specific paths — `grep -n "202[0-9]-[0-9][0-9]-[0-9][0-9]"` and `grep -n "C:\\\\|/home/"` against `metrics/headline.json` both returned no matches (grep exit 1).
- [x] no sensitive fields logged — `grep -n "log\|Logger\|logging"` across `lodestar/bench.py` and all five new `lodestar/commands/*.py` files returned nothing; none of the new code imports or calls the logging module. CLI commands only `typer.echo` scrubbed CUSTOMER-audience view data (or explicit `--audience internal` when the operator asks for it, per §15's documented flag).
- [x] tests named with RTM IDs — all 16 functions in `tests/test_bench.py` start with `test_fr017_`; all 11 functions in `tests/test_commands.py` start with `test_fr018_` (10) or `test_nfr006_` (1) — matching the pack's `rtm: [FR-017, FR-018, NFR-006]`.
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty.

## Findings
none
