# Verdict T-007 — PASS
Reviewed: 768b2253c59e  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — Independent in-memory MCP session (`mcp.shared.memory.create_connected_server_and_client_session`) lists exactly 6 tools: `get_claims_check, get_expansion_pipeline, get_health_score, get_metric_trend, get_next_actions, get_risks`. Called each with default audience; independent leak walker (re-implemented, not reusing `tests/leakwalk.py`) over `HealthReport`/`ClaimCheck`/`Risk`/`BrandProfile`/`NextAction` found zero populated internal fields (`LEAK FINDINGS (customer default): []`). Also confirmed non-vacuous via `tests/test_mcp.py::test_nfr005_internal_tool_results_are_non_vacuous` (internal audience does populate fields). `tests/test_mcp.py` (7 tests) all pass.
- AC2: PASS — `get_metric_trend("p95_latency_ms")` returned 31 values, 31 dates, `l7_mean = 1463.142857142857` (within ±0.1 of 1463.1). Invalid metric `"not_a_real_metric"` returned `isError: True` (MCP input-schema/Literal validation rejects it before the tool body runs).
- AC3: PASS — With `LODESTAR_INTERNAL_ENABLED=0` and cache reset, `get_health_score(audience="internal")` returned an MCP error result (`isError: True`) with text `"Error executing tool get_health_score: INTERNAL_DISABLED: internal audience is disabled (LODESTAR_INTERNAL_ENABLED=0)"` — contains `INTERNAL_DISABLED`. API: `GET /api/internal/health` under the same gate returned HTTP 403 with body `{"code": "INTERNAL_DISABLED", ...}`.
- AC4: PASS — `fastapi.testclient` hit all six routes (`health, trends, claims, risks, pipeline, actions`) for both `customer` and `internal` audiences — all 200. `/healthz` → 200 `{"status":"ok"}`. `/openapi.json` paths include `/healthz` and all six `/api/{audience}/<suffix>` routes. `/api/customer/health` body has `internal_composite: None` and `relationship_inputs: None`. `lodestar/api.py:26-35` (`_STATUS_BY_CODE`/`_status_for`) maps `Code.INTERNAL_DISABLED → 403`, `Code.PROVIDER_HTTP → 502`, all other codes (input/config) → default 400 — confirmed structurally by reading the source and confirmed behaviourally for the 400 and 403 cases (`GET /api/customer/trends?metric=bogus` → 400, `code: DATA_VALUE`; gated internal → 403).
- AC5: PASS — `uv run lodestar --help` output lists both `api` ("Run `uvicorn lodestar.api:app` on the configured (or given) port.") and `mcp` ("Run the Lodestar MCP server (stdio transport).") under Commands.

## Verification checklist
- [x] scope respected — `git diff HEAD~1 --stat`: `docs/mcp.md`, `docs/tasks/T-007-mcp-api.md` (handoff), `lodestar/api.py`, `lodestar/commands/api.py`, `lodestar/commands/mcp.py`, `lodestar/mcp_server.py`, `tests/test_api.py`, `tests/test_mcp.py` — matches pack Scope exactly. `tests/conftest.py` shows no diff (untouched); `tests/leakwalk.py` (used by both new test files) predates this task (`git log --follow` shows it was added in the T-003 commit, `de44de9`), consistent with the pack's fallback instruction.
- [x] error taxonomy used and mapped to HTTP codes — `lodestar/api.py` raises/handles `LodestarError` via a single `@app.exception_handler`, mapping `INTERNAL_DISABLED→403`, `PROVIDER_HTTP→502`, else `400`, body always `{code, message, detail}` per LLD §7/§13; verified behaviourally above.
- [x] no sensitive fields logged — `grep -n "log\|print("` over `lodestar/mcp_server.py`, `lodestar/api.py`, `lodestar/commands/mcp.py`, `lodestar/commands/api.py` returns no matches; no new logging introduced by this task's diff.
- [x] tests named with RTM IDs — all 7 tests in `tests/test_mcp.py` and all 12 in `tests/test_api.py` are prefixed `test_fr015_*`, `test_nfr005_*` (mcp) and `test_fr016_*`, `test_nfr005_*` (api), matching the pack's `rtm: [FR-015, FR-016, NFR-005]`.
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty; `mcp`, `fastapi`, `uvicorn` were already present in `pyproject.toml` dependencies before this task.

## Findings
1. none

## Full-suite evidence
- `uv run pytest tests/test_mcp.py tests/test_api.py -q` → 26 passed.
- `uv run mypy lodestar` → Success: no issues found in 21 source files.
- `uv run ruff check .` → All checks passed. `uv run ruff format --check .` → 55 files already formatted.
- `uv run python scripts/check.py` → ruff check, ruff format --check, mypy (native + `--platform linux`), pytest (168 passed, 97.59% coverage, 80% gate met), secrets scan — all checks passed.
- `uv run lodestar --help` → lists `api`, `mcp` (and pre-existing `dashboard`).
- Orchestrator ruling applied: the declared deviation (`functools.cache` in `lodestar/mcp_server.py:54` instead of `functools.lru_cache`) is accepted; behaviourally identical (`cache_clear()` used by `reset_cache()` at line 73, confirmed working across all gate-reset probes above).
