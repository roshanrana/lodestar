# Verdict T-012 — PASS
Reviewed: 8daf11b11981  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — Independent AppTest run: customer page `st.title` value is exactly `'Customer · Account health'` (U+00B7 confirmed by codepoint check); internal page title unchanged at `'Internal · QBR'`. Source: app/pages/customer_health.py:52, app/pages/internal_qbr.py:343.
- AC2: PASS — `_CHART_MARGIN` in app/components/charts.py:35-37 sets `t: 60` (raised from 40), applied via `_apply_common_layout` to every `line_with_incidents`/`dual_axis` figure. Verified on live internal-page chart specs (reliability, quality): `layout.margin == {l:10,r:10,t:60,b:10}` with `legend.y == 1.02`, satisfying the "top margin ≥ 60" branch of AC2. tests/test_charts.py::test_fr011_figures_use_the_frozen_layout_constants asserts `margin.t == 60` directly.
- AC3: PASS — Independent AppTest render of app/pages/home.py: full rendered markdown/caption text contains neither "margin" nor "relationship" (checked case-insensitively). New sentence reads "...an internal QBR view for the Fireworks account team." (app/pages/home.py:9-13).
- AC4: PASS — Live internal-page chart specs confirm `yaxis2` present in both the reliability chart (traces "Availability (%)" / "Error rate (%)") and the quality chart (traces "Grounded answer rate (%)"/"Quality eval pass rate (%)" / "CSAT"), both built via `dual_axis` (app/pages/internal_qbr.py:145-197); adoption chart correctly stays single-axis (`line_with_incidents`, no yaxis2). Weakest-pillar tile: `st.metric` label = "Weakest pillar" (14 chars, ≤32, no "…"), value = "65.8" (score, no "…"); pillar name ("Spend Efficiency") moved to the caption per the pack's "or caption" option (app/pages/internal_qbr.py:70-76, app/components/tiles.py:43-54). tests/test_page_internal.py::test_fr010_reliability_and_quality_charts_have_a_secondary_y_axis and ::test_fr010_weakest_pillar_tile_has_no_ellipsis_and_a_short_label both pass and match independent probe.
- AC5: PASS — Independent AppTest: `expander["Known caveats"].proto.expanded is True`, `expander["How the health score is calculated"].proto.expanded is False`. Source: app/pages/customer_health.py:182 (`st.expander("Known caveats", expanded=True)`); methodology expander in internal_qbr.py:93 unchanged (default collapsed).
- AC6: PASS — Committed docs/ebr/northstar-ebr-brief.md header line reads "**Deployment:** northstar-flagship-support-ft-v1". Talking point 3 reads "Availability averaged 99.85% and sat below the proposed 99.9% SLO on 27 of 31 days..." (two-decimal availability + days-below-SLO clause both present). Regenerated the brief independently via `uv run lodestar ebr --provider offline --out <tmp>` and diffed against the committed file with CRLF stripped from both sides: byte-for-byte identical. `uv run pytest tests/test_ebr.py -q` → 14 passed, including the determinism test.
- AC7: PASS on all sub-items —
  - lodestar/data.py module docstring (lines 1-5) now reads "missing file, oversized file, header schema, row-count cap, per-cell parsing, multiple deployments, duplicate dates," matching the actual `load()` order confirmed by reading the exception-raise sequence at lines 216-274 (DATA_NOT_FOUND → DATA_TOO_LARGE(size) → DATA_SCHEMA → DATA_TOO_LARGE(rows) → `_parse_row` per-cell → DATA_MULTI_DEPLOYMENT → DATA_DUPLICATE_DATE).
  - tests/test_app_skeleton.py tags are `fr001`/`fr018` (test_fr001_home_page_renders_without_exception_and_has_usage_chart, test_fr018_customer_health_stub_..., test_fr018_internal_qbr_stub_...) and each body asserts exactly what its name claims (loads without exception + has a plotly chart). Matches T-000's own RTM id list.
  - tests/test_incidents.py:43 asserts the literal `summary.days_below_slo == 27`.
  - tests/test_stats.py:34-35 asserts literal `window_sum(...) == 97_488` and `== 143_430`.
  - tests/test_ebr.py:113 test formerly `test_ac6_fr014_...` is now `test_fr014_ac6_rendered_figures_match_the_customer_view` (starts with `test_fr014_`).
  - Full gate: `uv run python scripts/check.py` → ruff, ruff format, mypy (x2), pytest (251 passed), secrets scan, bench, bench drift, card drift all report "all checks passed"; coverage 97.47% (≥80% floor).

## Verification checklist
- [x] scope respected — `git diff HEAD~1 --stat` (18 files) matches the pack's Scope list exactly: app/components/charts.py, app/pages/{customer_health,home,internal_qbr}.py, lodestar/{data,view}.py, lodestar/narrative/ebr.py, docs/ebr/northstar-ebr-brief.{md,docx}, and the eight named test files, plus docs/tasks/T-012-polish-followups.md (Handoff section only). No metrics/headline.json, docs/assets/metrics.svg, or README.md appear in the diff.
- [x] no business logic changed — lodestar/view.py diff adds exactly one field (`deployment: str`, customer-tagged) sourced from `ds.deployment`; lodestar/data.py diff is docstring-only; lodestar/narrative/ebr.py diff adds a `_fmt_pct2` formatter and repoints two placeholder sources/formats only; app/components/charts.py changes one margin constant; app/pages/*.py changes are strings, an expander flag, a tile call, and swapping `line_with_incidents`→`dual_axis` for two chart calls (layout only, same underlying trend data).
- [x] no sensitive fields logged — no new `print`/`logging`/`logger` calls introduced in the diff (grep of the full HEAD~1 diff for these patterns returned nothing).
- [x] tests named with RTM IDs — all new/modified test names retain or add `fr0xx`/`ac6` RTM-style prefixes (fr001, fr018, fr003, fr006, fr009, fr010, fr011, fr014); none regressed to untagged names.
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty.

## Findings
1. (non-blocking) The declared deviation — the internal reliability chart's SLO reference line (`hline=view.incidents.slo_availability_pct`, label "SLO") is dropped now that the chart uses `dual_axis`, which has no `hline` parameter (app/pages/internal_qbr.py:145-159) — is confirmed present in the diff exactly as declared in the Handoff. Per orchestrator ruling this is accepted and recorded as a backlog item; not scored against this verdict.
