# Verdict T-000 — PASS (after isolated re-verification; see addendum)
Reviewed: 07dcf5b (working tree)  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: FAIL — `uv run python scripts/check.py` does not exit 0 as currently measured. Re-run three times during this review, it failed at three different steps in succession as extra files appeared under `lodestar/` mid-review: (1) coverage 78.28% < 80% (`lodestar/spend.py` 0/32, `lodestar/stats.py` 0/18 covered), (2) `mypy lodestar` — 2 errors in `lodestar/incidents.py:35,45`, (3) `ruff format --check` — reformat needed in `lodestar/health.py:184`. None of `spend.py`, `stats.py`, `incidents.py`, `health.py` are in T-000's Scope list; they match LLD §3/§4/§6 modules belonging to a later task (T-002) and were not present when this review began (confirmed via an earlier `find lodestar -type f` that listed only the 8 scoped modules). On the files T-000 actually owns, `ruff check .` exits 0 and `mypy lodestar` was clean in the first pass of this review ("Success: no issues found in 9 source files" for both host and `--platform linux`) before the extraneous files appeared.
- AC2: PASS — independently recomputed from `data/northstar_flagship_30_day_metrics.csv` with a standalone script (not the implementer's tests): 31 rows, single deployment `northstar-flagship-support-ft-v1`, `requests_per_ticket` first=4.345230 last=6.548184, `tokens_per_request` first=1398.0, `cost_per_1m_tokens_usd` first=1.116291, `week` day1=1 day31=5, `note_kind` 2026-08-09/2026-08-21=incident, 2026-08-24=improvement, 2026-08-01/2026-08-15/2026-08-31=none. All within spec tolerance and matching `lodestar/data.py`'s implementation.
- AC3: PASS — `tests/test_data.py` covers all 7 required fixtures with correct `Code`/`detail` assertions: missing file (L45, `DATA_NOT_FOUND`), >10000 rows (L53, `DATA_TOO_LARGE`, detail.rows=10001), missing column (L72, `DATA_SCHEMA`, detail.missing), extra column (L81, `DATA_SCHEMA`, detail.extra), non-numeric cell (L92, `DATA_VALUE`, detail.row=2/column="requests"), two deployments (L105, `DATA_MULTI_DEPLOYMENT`), duplicate date (L117, `DATA_DUPLICATE_DATE`). All pass in `uv run pytest tests -q` (25 passed).
- AC4: PASS — `uv run lodestar --help` lists `dashboard` (verified directly). `tests/test_cli.py:22` (`test_fr018_new_command_module_is_discovered_without_editing_cli`) prepends a temp package path with a fake command module and confirms it is registered without editing `cli.py`; passes.
- AC5: PASS — `tests/test_app_skeleton.py` runs `AppTest.from_file` against `app/pages/home.py`, `customer_health.py`, `internal_qbr.py`; each asserts `not at.exception` and `len(at.get("plotly_chart")) >= 1`. All three pages share `app/components.py`'s `usage_chart()` (a real `plotly.graph_objects.Figure`) over `load_dataset()`. 3/3 pass.
- AC6: PASS — `uv run python scripts/secrets_scan.py` run directly against the repo prints "no secrets found" (exit 0). `tests/test_config.py:88-100` exercise the `FIREWORKS_API_KEY=fw_`+20 chars pattern and the `sk-ant-` pattern against temp files and assert exit code 1.
- AC7: PASS — `.env.example` lists all 10 LLD §8 variables with comments, matching the table exactly (verified by direct read). `.gitignore` covers `.venv/`, `.env`, `__pycache__/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `.coverage`, `coverage.xml`, `htmlcov/`.
- AC8: FAIL (as currently observed) — `mypy lodestar` failed with 2 errors during this review, both in `lodestar/incidents.py:35,45`, a file outside T-000's Scope that appeared mid-review (see AC1). On T-000's own scoped modules, mypy strict was clean earlier in this review before the extraneous file appeared. `app/` is correctly excluded from mypy (`[tool.mypy] packages = ["lodestar"]`, no `app` entry) and has no exclusion in `[tool.ruff] extend-exclude`, so it is included in ruff — matching the AC.
## Verification checklist
- [ ] scope respected — FAIL. `git status --short` shows only Scope-list files as new/modified for T-000, and `metrics/render.py` is untouched. However, during this review four extra untracked files appeared inside `lodestar/` that are not on T-000's Scope list and are not mentioned in its Handoff: `spend.py`, `stats.py`, `incidents.py`, `health.py` (LLD §3/§4/§6 — task T-002's module set). They are live in the working tree at time of review and are the direct cause of the AC1/AC8 failures above. Declared deviations (dropped `readme` from `pyproject.toml`; added `docs` and `scripts/shipyard` to ruff `extend-exclude`) are reasonable and consistent with the `metrics/render.py` precedent — `docs` contains no `.py` files (exclusion is inert) and `scripts/shipyard` contains two pre-existing out-of-scope `.py` files (`evidence.py`, `validate_task_pack.py`).
- [x] error taxonomy used — within T-000's scoped files every stdlib exception is caught and re-raised as `LodestarError` with a `Code` (`lodestar/data.py` `_value_error`, `_validate_header`, `load()`); no bare `ValueError`/`OSError` escapes `load()`. The `assert isinstance(...)` calls (`data.py` L202-206, 269) are internal type-narrowing after successful parsing, not validation paths, and are not escapes of unvalidated input.
- [x] no sensitive fields logged — no logging calls exist anywhere under `lodestar/` (grep for `print(`/`logging.`/`logger.`/`log.` found none); `Settings` never exposes API keys except as plain fields never written to output.
- [x] tests named with RTM IDs — all test functions use `test_<rtmid>_...` naming. Non-blocking note: `tests/test_app_skeleton.py` also tags tests `nfr002`, `fr010`, `fr011`, none of which are in T-000's declared `rtm:` front-matter (`[FR-001, FR-002, FR-018, NFR-003, NFR-004, NFR-007, NFR-008, NFR-009]`); FR-010/FR-011 are the full customer/internal dashboard requirements (only stubs exist here) and the NFR-002 test never measures timing, only no-exception + chart presence.
- [x] dependencies within the allowed list — `pyproject.toml` dependencies and dev group match the allowed list exactly (pydantic>=2.7, typer>=0.12, streamlit>=1.38, plotly>=5.22, pyyaml>=6, httpx>=0.27, fastapi>=0.111, uvicorn>=0.30, mcp>=1.9,<2, python-docx>=1.1; dev: ruff, mypy, pytest, pytest-cov, types-PyYAML).
- [x] CI workflow present — `.github/workflows/check.yml` runs on `ubuntu-latest` with `astral-sh/setup-uv@v5`, `uv python install 3.12`, `uv sync --all-extras`, `uv run python scripts/check.py` — matching AC1's requirement and check.py's own step list.
## Findings
1. (blocking) `uv run python scripts/check.py` does not exit 0 as currently measured (AC1 requires exit 0), and `mypy lodestar` fails (AC8). Root cause traced to `lodestar/spend.py`, `stats.py`, `incidents.py`, `health.py` — files outside T-000's Scope matching a later task's (T-002) LLD modules — appearing in the shared working tree during this review (confirmed absent at the start of the review, present by its end, with file birth-times inside the review window). This indicates concurrent write activity into the same working directory used for this T-000 review. It must be resolved (workspace isolation per task, or removal/relocation of the extraneous files) and `scripts/check.py` re-run clean on a tree containing only T-000's Scope files before this task can be certified PASS. On the files T-000 itself owns, all steps (ruff check, mypy, pytest, secrets scan) pass.
2. (non-blocking) `lodestar/data.py` module docstring (L3-6) states validation order as "header schema, per-cell parsing, row-count cap" but `load()` actually checks the row-count cap before per-cell parsing (L216-254: header → row-count cap → parse rows → multi-deployment → duplicate date). Documentation-only inconsistency; does not affect AC3's fixtures or behavior.
3. (non-blocking) `tests/test_app_skeleton.py` RTM tags `nfr002`/`fr010`/`fr011` are outside T-000's declared RTM list and, for NFR-002, the test does not exercise the actual startup-timing requirement it's named for (only checks no-exception + a chart renders). Consider re-tagging with T-000's own RTM ids, or adding a real timing assertion once NFR-002 is implemented.

## Addendum — orchestrator, T0 evidence (2026-09-10)
Finding 1 was environmental: the Verifier ran in the shared working tree while the Wave 1
Implementers (T-001, T-002) were writing new modules. T-000's scoped files were pinned at commit
e1720f4b7c9e and the full gate was re-run in an isolated git worktree at that commit:
`uv run python scripts/check.py` → ruff, ruff format, mypy (host + linux), pytest 25 passed,
coverage 95%, secrets scan: exit 0. AC1 and AC8 therefore PASS on T-000's own tree. Findings 2
and 3 (docstring order; RTM tags in test_app_skeleton.py) are carried as follow-ups to T-010.
Process change recorded in STATE.md: every later verification runs in a worktree pinned to the
task's commit, never in the live tree.
