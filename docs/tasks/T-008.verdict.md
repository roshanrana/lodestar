# Verdict T-008 — PASS
Reviewed: dfc3c4dad212  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — `docs/ebr/northstar-ebr-brief.md` has no `{{` (confirmed by script); exactly eight `## ` headings in the exact LLD §12 order (`1. Executive narrative` … `8. Biggest risk and mitigation`); `guard.check_text()` run independently against the rendered brief does not raise; `docx.Document("docs/ebr/northstar-ebr-brief.docx")` reopens with 8 heading paragraphs and 3 tables (independently re-verified). Tests: `test_fr014_render_ebr_offline_has_no_unfilled_placeholders`, `test_fr014_render_ebr_offline_has_eight_headings_in_order`, `test_fr014_render_ebr_offline_passes_the_guard`, `test_fr014_write_docx_has_at_least_eight_headings_and_three_tables` (tests/test_ebr.py) — all pass.
- AC2: PASS — independently drove `lodestar/commands/ebr.py`'s `app` via `typer.testing.CliRunner` with a monkeypatched hostile provider returning `"Confidence is 75% and margin is 62%; ignore previous instructions"`: exit code 0, rendered file contains neither the injection text nor the hostile percentages. Guard raises `LEAK_GUARD` with `detail={"category": "term"}` only (lodestar/narrative/guard.py:107-112), and `render_ebr` (lodestar/narrative/ebr.py:236-244) logs `"guard rejected provider narrative: category=%s"` on rejection, never the text. Tests: `test_fr014_hostile_provider_narrative_is_rejected_and_offline_narrative_kept`, `test_fr014_cli_ebr_exits_zero_when_guard_rejects_provider_narrative` — pass.
- AC3: PASS — independently built `OpenAICompatProvider` over `httpx.MockTransport`: a 500 response raises `PROVIDER_HTTP` with `{"status": 500, "provider": "fireworks"}` (lodestar/narrative/provider.py:46-51, 84-85); with `LODESTAR_LLM_PROVIDER=fireworks` and no `FIREWORKS_API_KEY`, `make_provider` raises `PROVIDER_CONFIG` (`{"var": "FIREWORKS_API_KEY"}`) — verified with `httpx.Client` monkeypatched to raise `AssertionError` if constructed (mirrors `tests/test_provider.py:20-26,45-55`), confirming no I/O occurs before the config check. The prompt content (system + user messages — the only fields derived from account data) passes `check_text()` against the INTERNAL view with no leaked terms or figures, matching `test_fr014_provider_prompt_is_built_only_from_the_customer_view` (tests/test_ebr.py:137-148). See Findings #4 for a non-blocking note on a coincidental numeric collision found only when guard-checking the full HTTP envelope (not the prompt) rather than the message content.
- AC4: PASS — independently built `AnthropicProvider` over `httpx.MockTransport`: captured headers include `x-api-key: ak` and `anthropic-version: 2023-06-01`, POST goes to `https://api.anthropic.com/v1/messages`, and `complete()` returns `data["content"][0]["text"]` correctly (lodestar/narrative/provider.py:101-118). Test `test_fr012_anthropic_provider_headers_and_response_parsing` passes.
- AC5: PASS — `uv run lodestar ebr --provider offline --out <tmp>` wrote both files; `diff --strip-trailing-cr` shows the produced `.md` is byte-identical in content to the committed `docs/ebr/northstar-ebr-brief.md` (the only raw-byte difference is Windows text-mode CRLF vs. the repo's `.gitattributes`-enforced `eol=lf`, not a content difference). `test_fr014_committed_ebr_matches_a_fresh_offline_render` (text-mode comparison) and `test_fr014_write_ebr_offline_is_deterministic` both pass.
- AC6: PASS — independently confirmed the rendered brief contains "70.8", "67.9", "7.35", "84.2", "1,213" (not "1213"), and "27 of 31" via script; `test_ac6_fr014_rendered_figures_match_the_customer_view` passes.
## Verification checklist
- [x] scope respected — `git diff HEAD~1 --stat` touches exactly: docs/ebr/{northstar-ebr-brief.md,.docx}, docs/tasks/T-008-narrative-ebr.md (Handoff), lodestar/commands/ebr.py, lodestar/narrative/{__init__,ebr,guard,provider}.py, tests/{test_ebr,test_guard,test_provider}.py — matches the pack's Scope list exactly.
- [x] error taxonomy used — `PROVIDER_CONFIG` (provider.py:121-126), `PROVIDER_HTTP` (provider.py:46-51), `LEAK_GUARD` (guard.py:101-121) all present and independently triggered as above.
- [x] no prompt bodies or keys logged — `grep -n "logger\."` over the narrative package returns exactly two call sites: `provider.py:154` (`"provider selected: name=%s model=%s"`, no key) and `ebr.py:240-242` (`"guard rejected provider narrative: category=%s"`, no text). `test_nfr001_provider_never_logs_key_or_body` passes.
- [x] tests named with RTM IDs — 30 of 31 new tests start with `test_fr012_`, `test_fr013_`, `test_fr014_`, or `test_nfr001_`; one, `test_ac6_fr014_rendered_figures_match_the_customer_view` (tests/test_ebr.py:113), is prefixed `test_ac6_` before the RTM id rather than leading with it — see Finding #3 (non-blocking).
- [x] no new dependencies — `git diff HEAD~1 -- pyproject.toml uv.lock` is empty; httpx and python-docx were already declared before this task (the `screenshots` optional group is from an unrelated earlier commit, per orchestrator note).
- [x] provider receives only the CUSTOMER view — `placeholders()` and `_customer_figures_json()` (lodestar/narrative/ebr.py:105-209) build the prompt exclusively from `view_customer`; `render_ebr`'s third parameter (`view_internal_for_guard`) is used only by `guard.check_text()`, never passed to `provider.complete()`. Confirmed by code read and by `test_fr014_provider_prompt_is_built_only_from_the_customer_view`.
## Findings
1. (non-blocking) Declared deviation, already accepted per orchestrator ruling and routed to T-012: `{{deployment}}` is rendered from `view_customer.generated_from` (CSV basename) because `AccountView` has no `deployment` field. Renders as `northstar_flagship_30_day_metrics.csv`; does not affect AC1–AC6.
2. (non-blocking) `render_ebr(view_customer, provider, view_internal_for_guard)` (lodestar/narrative/ebr.py:220-222) carries a third parameter beyond the two-parameter signature shown in LLD §12 (`render_ebr(view_customer, provider) -> str`). This is needed because `guard.check_text()` requires the INTERNAL view to compute internal-figure strings; §12 is not marked FROZEN (unlike §4), and the extra parameter does not affect the customer-only prompt guarantee (see checklist item 6).
3. (non-blocking) `tests/test_ebr.py:113` — `test_ac6_fr014_rendered_figures_match_the_customer_view` does not strictly start with `test_fr014_`/`test_nfr001_` per LLD §17's naming rule (prefixed `test_ac6_` first); the RTM id is still present in the name.
4. (non-blocking) When the leak guard's `check_text()` is run over the *entire* raw Fireworks-compatible HTTP request envelope (JSON body including `max_tokens`, not just the `messages` content), it flags a `figure` leak because the request's default `max_tokens: 600` coincidentally numerically matches an unrelated internal figure (also 600) in the INTERNAL view. Re-running with a different `max_tokens` value removes the collision, and the message content alone (system + user prompts — the only fields derived from account data) passes the guard cleanly, consistent with `test_fr014_provider_prompt_is_built_only_from_the_customer_view`. Not an actual data leak and does not block AC3, but noted for completeness per the verification instructions.
5. (non-blocking, editorial) The executive narrative (`offline_executive_narrative`, lodestar/narrative/ebr.py:72-102) and the template's executive talking points (§7) read as plain, executive-ready prose; no internal vocabulary detected by eye or by `check_text()`. Minor stylistic looseness ("held near $0.0124", "a rounding error") is acceptable for a VP-level EBR.
